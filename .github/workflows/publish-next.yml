name: Publish next video

on:
  schedule:
    # Four local runs a day, away from GitHub's busy top-of-hour window.
    # 00:17/06:17/12:17/18:17 Asia/Kathmandu = 18:32/00:32/06:32/12:32 UTC.
    - cron: "32 0,6,12,18 * * *"
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Render one video only; never touch Postiz or state.json"
        type: boolean
        default: true

permissions:
  contents: write

concurrency:
  # Share the group with the manual batch workflow so they never overlap.
  group: render
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      DRY_RUN: ${{ github.event_name == 'workflow_dispatch' && inputs.dry_run && '1' || '0' }}

    steps:
      - uses: actions/checkout@v4
        with:
          # workflow_dispatch lets callers choose a branch. Production secrets
          # must only ever be used with the reviewed code on main.
          ref: main
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - id: queue
        name: Pick the next item from accepted weekly plans
        shell: bash
        run: node scripts/queue.mjs --github-output >> "$GITHUB_OUTPUT"

      - name: Queue is empty
        if: steps.queue.outputs.id == ''
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        shell: bash
        run: |
          echo "::warning::Queue is empty. Submit a new 28-item weekly plan."
          if [ -n "$TELEGRAM_BOT_TOKEN" ] && [ -n "$TELEGRAM_CHAT_ID" ]; then
            curl -fsS -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
              -d chat_id="$TELEGRAM_CHAT_ID" \
              -d disable_web_page_preview=true \
              -d text="Video queue is empty. Submit a new 28-item weekly plan.
          $RUN_URL" || true
          fi

      - name: Install Chrome runtime libraries
        if: steps.queue.outputs.id != ''
        shell: bash
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
            libnss3 libdbus-1-3 libatk1.0-0 libatk-bridge2.0-0 \
            libcups2 libxkbcommon0 libxdamage1 libgbm1 libpango-1.0-0 \
            libcairo2 fonts-liberation || true
          sudo apt-get install -y libasound2t64 || sudo apt-get install -y libasound2 || true

      - name: Install dependencies
        if: steps.queue.outputs.id != ''
        run: npm install --no-audit --no-fund

      - name: Cache Chrome Headless Shell
        if: steps.queue.outputs.id != ''
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/remotion
            node_modules/.remotion
          key: remotion-browser-${{ runner.os }}-v1

      - name: Ensure browser
        if: steps.queue.outputs.id != ''
        run: npx remotion browser ensure

      # The soundtrack is synthesised, not licensed, so it is a build artifact
      # rather than committed binaries. Deterministic: the same cue list always
      # produces the same bytes.
      - name: Build the audio pack
        if: steps.queue.outputs.id != ''
        run: node scripts/build-audio.mjs

      - name: Render and publish one video
        if: steps.queue.outputs.id != ''
        env:
          POSTIZ_API_URL: ${{ secrets.POSTIZ_API_URL }}
          POSTIZ_API_KEY: ${{ secrets.POSTIZ_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          ONLY: ${{ steps.queue.outputs.id }}
          PLAN_PATH: ${{ steps.queue.outputs.plan_path }}
          QUEUE_RUN: "1"
          # Uploads the finished MP4 to the week's GitHub Release. Covered by
          # the contents: write permission this workflow already holds.
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # REMOTION_CONCURRENCY is deliberately unset: one video per run means
          # remotion.config.ts can give it every core the runner has.
        run: node scripts/render-all.mjs

      - name: Commit queue state and archive record
        if: steps.queue.outputs.id != '' && env.DRY_RUN != '1'
        env:
          ITEM_ID: ${{ steps.queue.outputs.id }}
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -- state.json
          if [ -f archive/manifest.json ]; then
            git add -- archive/manifest.json
          fi

          # The video bytes live in a Release, never in git. Anything else
          # showing up staged means something wrote where it should not have.
          unexpected="$(git diff --cached --name-only \
            | grep -vx -e 'state.json' -e 'archive/manifest.json' || true)"
          if [ -n "$unexpected" ]; then
            echo "Refusing to commit unexpected paths:" >&2
            echo "$unexpected" >&2
            exit 1
          fi

          if git diff --cached --quiet; then
            echo "Nothing to commit."
            exit 0
          fi

          git commit -m "chore(queue): mark $ITEM_ID posted"
          for attempt in 1 2 3; do
            if git push origin HEAD:main; then
              exit 0
            fi
            echo "Push attempt $attempt failed; rebasing onto current main."
            git pull --rebase origin main
          done
          echo "Could not push state.json after three attempts." >&2
          exit 1

      - name: Archive to Cloudflare R2
        if: steps.queue.outputs.id != '' && success()
        continue-on-error: true
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: auto
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
        shell: bash
        run: |
          if [ -z "$AWS_ACCESS_KEY_ID" ] || [ -z "$AWS_SECRET_ACCESS_KEY" ] || \
             [ -z "$R2_ENDPOINT" ] || [ -z "$R2_BUCKET" ]; then
            echo "::warning::R2 is incomplete, skipping archive."
            exit 0
          fi
          aws s3 sync out/ "s3://$R2_BUCKET/$(date +%Y-%m)/" \
            --endpoint-url "$R2_ENDPOINT" --exclude "*.props.json"

      - name: Keep the video for 7 days
        if: always() && steps.queue.outputs.id != ''
        continue-on-error: true
        uses: actions/upload-artifact@v4
        with:
          name: video-${{ steps.queue.outputs.id }}-${{ github.run_id }}
          path: |
            out/*.mp4
            out/summary.json
          retention-days: 7
          if-no-files-found: warn

  notify-failure:
    needs: publish
    if: ${{ always() && needs.publish.result == 'failure' }}
    runs-on: ubuntu-latest
    steps:
      - name: Telegram
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        shell: bash
        run: |
          [ -z "$TELEGRAM_BOT_TOKEN" ] && exit 0
          curl -fsS -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
            -d chat_id="$TELEGRAM_CHAT_ID" \
            -d disable_web_page_preview=true \
            -d text="Publish-next run failed. Check Postiz before retrying: a failure during the state push can mean Postiz already accepted the video.
          $RUN_URL" || true
